[
  {
    "name": "Today's remaining capacity",
    "steps": [
      {
        "send": { "text": "/admin" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "Manage today’s reservations and restaurant settings." } }]
      },
      {
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "Today has 36 seats remaining. 0 of 36 seats are booked."
            }
          }
        ],
        "send": {
          "callback": "admin:capacity"
        }
      }
    ]
  }
]
