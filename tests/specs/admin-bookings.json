[
  {
    "name": "Upcoming bookings",
    "steps": [
      {
        "send": { "text": "/admin" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "Manage today’s reservations and restaurant settings." } }]
      },
      {
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "No upcoming bookings yet — tables are ready to reserve."
            }
          }
        ],
        "send": {
          "callback": "admin:bookings"
        }
      }
    ]
  }
]
