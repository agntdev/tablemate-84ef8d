[
  {
    "name": "/help explains the bot in plain language",
    "steps": [
      {
        "send": { "text": "/help" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Tap Book a table to choose a date, time, and party size.\n\nUse My bookings to reschedule or cancel. Restaurant owners can open Owner tools with /admin."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "the Help menu button shows the same help text",
    "steps": [
      {
        "send": { "callback": "menu:help" },
        "expect": [{ "method": "editMessageText" }]
      }
    ]
  }
]
